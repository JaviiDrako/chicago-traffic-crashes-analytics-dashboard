# SQL workspace

Standalone BigQuery SQL files can be stored here when they are useful outside the dbt project, for example source profiling queries or ad hoc validation queries.

Production transformations should live in `dbt/models/` so that they are tested and documented.
