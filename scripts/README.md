# Scripts workspace

Repeatable local utilities belong here, such as schema inspection, checksum generation or controlled upload helpers.

Scripts must never contain credentials or hard-code personal access tokens.
